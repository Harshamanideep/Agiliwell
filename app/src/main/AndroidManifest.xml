<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Receivers -->
    <receiver
        android:name=".notification.YogaNotificationReceiver"
        android:exported="false" />
    <receiver
        android:name=".notification.ExerciseNotificationReceiver"
        android:exported="false" />

    <application
        android:name=".AgiliwellApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/agiliwell_logo"
        android:roundIcon="@mipmap/agiliwell_logo_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:hardwareAccelerated="true"
        android:theme="@style/Theme.Agiliwell"
        tools:replace="android:theme"
        tools:targetApi="31">

        <activity
            android:name=".AgiliwellActivity"
            android:exported="true"
            android:theme="@style/Theme.Agiliwell">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver android:name="com.app.agiliwell.alarm.default_alarm.AlarmReceiver" />
        <receiver android:name="com.app.agiliwell.notification.WaterNotificationReceiver" />

    </application>

</manifest>
